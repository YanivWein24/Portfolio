export interface SkillProps {
  name: string;
  color: string;
  link: string;
  img: string;
}

export interface FamiliarSkillProps {
  name: string;
  link: string;
  img: string;
  smallScreen?: Boolean;
}
